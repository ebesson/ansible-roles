---
- name: footloose | download and install footloose
  unarchive:
    src: "{{ footloose_url }}"
    dest: "{{ footloose_destination }}"
    remote_src: yes
    owner: root
    group: root
    mode: 755
  tags:
    - footloose
